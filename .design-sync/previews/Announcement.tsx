import { Announcement } from "yoga-by-delphine";

// Announcement is a fixed full-width promotional banner. The MemoryRouter
// provider supplies the router context its inner `Button as={Link}` needs.
export const Default = () => <Announcement />;
